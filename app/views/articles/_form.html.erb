<%= form_for(@article, html: {class: "article_form", id: "editor"}) do |f| %>
  <div class="title">
    <input type="text" name="article[title]" value="<%=@article.title%>">
  </div>
  <div class="content">
    <textarea v-model="content" name="article[content]"><%= @article.content %></textarea>
  </div>
  <div class="preview">
    <div v-html="mark(content)"></div>
  </div>
  <%= f.submit "投稿", class: "btn btn-primary"%>
<% end %>

 <script type="text/javascript">
   var vm = new Vue({
     el: "#editor",
     data: {
       content: "<%= j @article.content %>",
     },
     methods: {
       mark: function(text){
         return marked(text)
       },
     },
   });
   window.vm = vm;
 </script>
