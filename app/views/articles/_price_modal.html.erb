<div class="modal" id="price-modal">
      <div class="modal-header row">
        <h3 class="modal-title col">Set Price</h3>
        <div class="switch col">
          <label>
            <input type="checkbox" v-model="bool" v-on:change="FreeOrToll" id="toll" name="options" value="toll">有料
            <span class="lever"></span>無料
          </label>
        </div>
        <button type="button" class="modal-close btn-flat col">
          <span aria-hidden="true">&#215;</span><span class="sr-only">閉じる</span>
        </button>
      </div>
      <%= f.fields_for :price do |price_form| %>
      <div class="modal-body">
        <div class="controls">
          <div class="row">
            <div class="input-field col s12">
              <input type="text" v-model="minPrice" name="article[price_attributes][min]"
              class="margin-yen" id="min_price" placeholder="100~1000" :disabled="isDisabled">
              <span>円</span>
              <label for="min_price">開始価格</label>
            </div>
            <div class="input-field col s12">
              <input type="text" v-model="maxPrice" name="article[price_attributes][max]"
              class="margin-yen" id="max_price" placeholder="100~1000" :disabled="isDisabled">
              <span>円</span>
              <label for="max_price">最大価格</label>
            </div>
          </div>
          <div class="row">
            <div class="input-field col s24">
                <%= price_form.select :rate, enum_options_for_select(Price, :rate), {}, ":readonly": "isDisabled", "v-model": "rate" %>
                <label>レート</label>
            </div>
          </div>
        </div>
      <% end %>
      <div class="modal-footer">
        <input type="submit" class="btn" value="記事を投稿">
      </div>
    </div>

</div>
