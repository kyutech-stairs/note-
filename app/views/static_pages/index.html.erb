<div class="container">
  <div class="main_wrapper">
    <div class="row">
        <% if @articles.any? %>
          <%= render @articles %>
        <% end %>
    </div>

      <%= paginate @articles%>

  </div>
</div>
