<div class="main_wrapper">
  <div class="row">
      <% if @articles.any? %>
        <%= render @articles %>
      <% end %>
  </div>
  <div class="paginate">
    <%= paginate @articles%>
  </div>
</div>
