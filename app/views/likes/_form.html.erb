<% if current_user %>
  <% if current_user.liking?(@article)%>
    <%= link_to "", article_like_path(@article.likes.find_by(user_id: current_user.id),
                                      article_id: @article.id),
                                      class: "fa fa-heart like-button",
                                      method: :delete, remote: true %>
  <% else %>
    <%= link_to "", article_likes_path(@like, article_id: @article.id),
      class: "fa fa-heart-o like-button", 
      method: :post, remote: true %>
  <%end%>
  <div class="like-count">
    <%= @article.likes.count %>
  </div>
<% else %>
  <i class="fa fa-heart like-icon"></i>
  <div class="like-count">
    <%= @article.likes.count%>
  </div>
<% end %>

